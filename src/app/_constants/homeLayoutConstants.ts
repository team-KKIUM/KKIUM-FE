/** Full-width home dashboard blocks (grow with main content area). */
export const HOME_DASHBOARD_CONTENT_CLASS = 'w-full min-w-0';

/** Bottom row: only the experience-update column grows; job-type & bubble stay fixed width. */
export const HOME_DASHBOARD_BOTTOM_GRID_CLASS =
  'grid grid-cols-1 items-stretch gap-5 xl:grid-cols-[minmax(0,1fr)_384px_384px]';

export const HOME_DASHBOARD_SIDE_CARD_CLASS = 'w-full min-w-0 xl:max-w-[384px]';
